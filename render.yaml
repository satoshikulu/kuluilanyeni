services:
  - type: static
    name: kulu-ilan
    plan: free
    branch: main
    buildCommand: npm ci && npm run build
    publish: dist
    # Vite/React SPA yönlendirmesi: tüm yolları index.html'e düşür
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_SUPABASE_URL
        sync: false
      - key: VITE_SUPABASE_ANON_KEY
        sync: false
